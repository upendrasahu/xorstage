app:
  title: XorStage Demo
  baseUrl: ${APP_BASE_URL:-http://localhost:3000}

organization:
  name: Xorlabs

backend:
  # Enable authentication in the backend
  auth:
    keys:
      - secret: ${BACKEND_SECRET}
  baseUrl: ${BACKEND_BASE_URL:-http://localhost:7007}
  listen:
    port: 7007
    # Bind to all interfaces to be accessible from outside
    host: 0.0.0.0
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: ${CORS_ORIGIN:-http://localhost:3000}
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  database:
    client: better-sqlite3
    connection: ':memory:'
  
  # Allow reading from GitHub raw URLs
  reading:
    allow:
      - host: github.com
      - host: raw.githubusercontent.com

integrations:
  github:
    - host: github.com
      # This is a Personal Access Token or PAT from GitHub. You can find out how to generate this token, and more information
      # about setting up the GitHub integration here: https://backstage.io/docs/integrations/github/locations#configuration
      token: ${GITHUB_TOKEN}
      # We're adding the repositories from upendrasahu's organization to be scanned for catalog files
      repos:
        - upendrasahu/exercise_tracker
    ### Example for how to add your GitHub Enterprise instance using the API:
    # - host: ghe.example.net
    #   apiBaseUrl: https://ghe.example.net/api/v3
    #   token: ${GHE_TOKEN}

proxy:
  ### Example for how to add a proxy endpoint for the frontend.
  ### A typical reason to do this is to handle HTTPS and CORS for internal services.
  # endpoints:
  #   '/test':
  #     target: 'https://example.com'
  #     changeOrigin: true

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    # Try running locally instead of Docker to simplify file access
    runIn: 'local' # Changed from 'docker'
    # dockerImage: spotify/techdocs:v1.0.3 # Not needed if runIn is 'local'
    # pullImage: true # Not needed if runIn is 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.
  # For better TechDocs performance in demo environment
  cache:
    ttl: 600000 # 10 minutes in milliseconds
  
  # Add this section to handle GitHub repository references
  requestUrl: ${BACKSTAGE_HOST_URL}
  legacyUseCaseSensitiveTripletPaths: false
  # Disable GitHub api as it's causing 404s when trying to fetch docs
  disableGitHubApi: true # Keep this for now

auth:
  # see https://backstage.io/docs/auth/ to learn about auth providers
  providers:
    # See https://backstage.io/docs/auth/guest/provider
    guest: {}

    github:
      development:
        clientId: ${AUTH_GITHUB_CLIENT_ID}
        clientSecret: ${AUTH_GITHUB_CLIENT_SECRET}
        callbackUrl: ${AUTH_GITHUB_CALLBACK_URL}
        ## uncomment if using GitHub Enterprise
        # enterpriseInstanceUrl: ${AUTH_GITHUB_ENTERPRISE_INSTANCE_URL}
        ## uncomment to set lifespan of user session
        # sessionDuration: { hours: 24 } # supports `ms` library format (e.g. '24h', '2 days'), ISO duration, "human duration" as used in code
        signIn:
          resolvers:
            # See https://backstage.io/docs/auth/github/provider#resolvers for more resolvers
            - resolver: usernameMatchingUserEntityName

scaffolder:
  # see https://backstage.io/docs/features/software-templates/configuration for software template options
  # set to false to disable GitHub URL annotation on the task execution page
  # github.com configuration
  defaultAuthor:
    name: Upendra Sahu
    email: upendra.sahu@xoriant.com
  defaultCommitMessage: Initial commit from Backstage
  # don't use defaultOwner if you want to allow other org/user on the form
  #defaultOwner: upendrasahu

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location, User, Group]
  locations:
    # Local example data, file locations are relative to the backend process, typically `packages/backend`
    - type: file
      target: ../../examples/entities.yaml

    # Local example template
    - type: file
      target: ../../examples/template/template.yaml
      rules:
        - allow: [Template]

    # Local example organizational data
    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group]

    # Main catalog file from exercise_tracker repo
    - type: url
      target: https://raw.githubusercontent.com/upendrasahu/exercise_tracker/main/catalog-info.yaml
      rules:
        - allow: [Component, System, API, Resource, Location]
    
    # Users file from exercise_tracker repo with proper rules
    - type: url
      target: https://raw.githubusercontent.com/upendrasahu/exercise_tracker/main/users.yaml
      rules:
        - allow: [User]
    
    # Groups file from exercise_tracker repo with proper rules
    - type: url
      target: https://raw.githubusercontent.com/upendrasahu/exercise_tracker/main/groups.yaml
      rules:
        - allow: [Group]

  # Experimental: Always use the search method in UrlReaderProcessor.
  # New adopters are encouraged to enable it as this behavior will be the default in a future release.
  useUrlReadersSearch: true

kubernetes:
  # see https://backstage.io/docs/features/kubernetes/configuration for kubernetes configuration options

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: true
